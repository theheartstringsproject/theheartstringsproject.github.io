import React from 'react'

const WorldMap = (props) => (
	<ellipse className={props.className} /*fillOpacity={props.fillOpacity} fill={props.fill} */cx={props.cx} cy={props.cy} rx="0.930903078" ry="0.929681818"></ellipse>
)

export default WorldMap